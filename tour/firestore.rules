rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 匿名登入 uid 與文件 id 一致時才可讀寫自己的旅遊資料
    match /userTours/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
